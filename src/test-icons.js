const lucide = require('lucide-react');
console.log(Object.keys(lucide).filter(k => k.toLowerCase().includes('diff') || k.toLowerCase().includes('terminal') || k.toLowerCase().includes('git') || k.toLowerCase().includes('cloud')));
