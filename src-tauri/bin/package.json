{
  "name": "@picode/pi-runtime",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --compile --minify --sourcemap ./src/main.ts --outfile ../src-tauri/bin/pi-runtime",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@mariozechner/pi-coding-agent": "0.67.68"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
